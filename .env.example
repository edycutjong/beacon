# ── Beacon — Environment Variables ───────────────────────────
# This app runs 100% OFFLINE. No API keys required.
# All inference via @qvac/sdk (on-device or P2P delegated).

# ── P2P Provider (Optional) ─────────────────────────────────
# Host address if running provider on a specific interface:
# BEACON_PROVIDER_HOST=0.0.0.0
# BEACON_PROVIDER_PORT=9090
